# Task class is used to store the task information of the user
# Has the following member variables: task name, due date, difficulty, and sub tasks

class Task:

    # Constructor
    def __init__(self, name, due, difficulty):
        self.__name = name
        self.__due = due
        self.__difficulty = difficulty

    # Create Constructor that also stores subtasks

    # Create function that adds subtasks

    # Create Setters and Getters for Task variables
